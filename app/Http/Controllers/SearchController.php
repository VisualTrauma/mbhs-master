<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class SearchController extends Controller
{
    public function store()
	{
		// $value = Input::get('search');
		// $results = Document::where('body', 'like', '%' . $value . '%')->get();

		// $access_level = Get::accessLevel();
		// $sections = Get::sectionsWithDocuments();

		// return View::make('pages/search', ['sections' => $sections, 'access_level' => $access_level, 'results' => $results, 'value' => $value]);
	}
}
